{
  "rules": {
    ".read": false,
    ".write": false,

    "forecourt": {
      ".read": true,
      ".write": true
    },

    "disclosures": {
      ".read": "auth != null",
      "$disclosureId": {
        ".read": true,
        ".write": "!data.exists()"
      }
    },

    "rav_forms": {
      ".read": "auth != null",
      "$ravId": {
        ".read": true,
        ".write": "!data.exists()"
      }
    }
  }
}
