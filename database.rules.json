{
  "rules": {
    ".read": false,
    ".write": false,

    "forecourt": {
      ".read": true,
      ".write": true
    },

    "disclosures": {
      ".read": "auth != null",
      "$disclosureId": {
        ".read": true,
        ".write": "!data.exists()"
      }
    }
  }
}
